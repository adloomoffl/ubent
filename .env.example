# Google OAuth web application credentials (keep secrets on this computer).
GOOGLE_CLIENT_ID=
GOOGLE_CLIENT_SECRET=
NEXTAUTH_URL=http://localhost:3001
# Generate a long random secret. The local .env.local already has one.
NEXTAUTH_SECRET=
# Salted scrypt hash for username ubentertainment. Never store the plain password.
ADMIN_PASSWORD_HASH=
